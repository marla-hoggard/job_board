<section class="show">
<h1><%= @job.title %></h1>
<p><%= @job.description %></p>
<h5><%= link_to "Edit", edit_job_path(@job) %></h5>
<h5><%= link_to 'Delete', @job, method: :delete, data: { confirm: 'Are you sure?' } %></h5>
</section>
